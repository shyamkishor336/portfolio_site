export const Footer = () => import('../../../../components/Footer.vue' /* webpackChunkName: "components/footer" */).then(c => wrapFunctional(c.default || c))
export const PaperList = () => import('../../../../components/PaperList.vue' /* webpackChunkName: "components/paper-list" */).then(c => wrapFunctional(c.default || c))
export const ProjectList = () => import('../../../../components/ProjectList.vue' /* webpackChunkName: "components/project-list" */).then(c => wrapFunctional(c.default || c))
export const TopComponent = () => import('../../../../components/TopComponent.vue' /* webpackChunkName: "components/top-component" */).then(c => wrapFunctional(c.default || c))
export const TopComponentSmall = () => import('../../../../components/TopComponentSmall.vue' /* webpackChunkName: "components/top-component-small" */).then(c => wrapFunctional(c.default || c))

// nuxt/nuxt.js#8607
function wrapFunctional(options) {
  if (!options || !options.functional) {
    return options
  }

  const propKeys = Array.isArray(options.props) ? options.props : Object.keys(options.props || {})

  return {
    render(h) {
      const attrs = {}
      const props = {}

      for (const key in this.$attrs) {
        if (propKeys.includes(key)) {
          props[key] = this.$attrs[key]
        } else {
          attrs[key] = this.$attrs[key]
        }
      }

      return h(options, {
        on: this.$listeners,
        attrs,
        props,
        scopedSlots: this.$scopedSlots,
      }, this.$slots.default)
    }
  }
}
